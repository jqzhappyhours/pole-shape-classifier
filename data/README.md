# Local data (not in git)

Everything here is generated or downloaded on your machine.

| Path | Contents |
|------|----------|
| `clips/<shape>/` | Source videos for frame extraction |
| `images/<shape>/` | Frames from `extract.py` |


Recreate by running `extract.py` and the `collect_*` scripts from the repo root.
